include "common.fbs";
namespace sf;


table Texture {
	id         : uint64;
	file_name  : string;
	media_name : string;
	pixel_type : PixelType;
	image_type : ImageType;
	data       : [ubyte];
        /// for raw image
        width      : uint32;
        height     : uint32;
        components : uint32;
}
